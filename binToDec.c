//Program to convert binary number to decimal
#include <stdio.h>

int convert(int);

int main()
{
    int binary;

    printf("Enter a binary number: ");
    scanf("%d", &binary);

    printf("decimal : %d", convert(binary));

    return 0;
}

int convert(int x)
{

    return 0;
}